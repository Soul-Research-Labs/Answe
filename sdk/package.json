{
  "name": "@starkprivacy/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for StarkPrivacy — ZK privacy protocol on Starknet",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "starkprivacy": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "DEVNET_URL=http://127.0.0.1:5050/rpc vitest run",
    "lint": "tsc --noEmit",
    "docs": "typedoc",
    "prepublishOnly": "npm run lint && npm run test && npm run build"
  },
  "keywords": [
    "starknet",
    "privacy",
    "zk",
    "stark",
    "poseidon"
  ],
  "license": "MIT",
  "dependencies": {
    "starknet": "^6.23.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0",
    "typedoc": "^0.27.0"
  }
}
